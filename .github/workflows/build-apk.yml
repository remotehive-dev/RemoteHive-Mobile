name: Build APK

on:
  push:
    branches: [main]

permissions:
  contents: write

env:
  R2_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_ENDPOINT }}
  R2_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY }}
  R2_SECRET_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_KEY }}
  R2_BUCKET: ${{ secrets.CLOUDFLARE_R2_BUCKET }}
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dorg.gradle.daemon=false"
  ANDROID_HOME: C:\Users\dell\AppData\Local\Android\Sdk
  TMP: C:\tmp
  TEMP: C:\tmp
  ANDROID_TEMP: C:\tmp
  CMAKE_NINJA_FORCE_RESPONSE_FILE: "ON"

jobs:
  build:
    runs-on: [self-hosted, Windows, android]
    steps:
      - uses: actions/checkout@v4
        with:
          clean: false

      - name: Enable long paths for Git
        shell: powershell
        run: git config --global core.longPaths true

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install CMake 3.28 (fixes Windows ninja dirty-manifest)
        shell: powershell
        run: |
          $cmakeVersion = "3.28.3"
          $cmakeDir = "C:\cmake-$cmakeVersion"
          if (-not (Test-Path "$cmakeDir\bin\cmake.exe")) {
            $cmakeUrl = "https://github.com/Kitware/CMake/releases/download/v$cmakeVersion/cmake-$cmakeVersion-windows-x86_64.zip"
            $zipFile = "$env:TEMP\cmake-$cmakeVersion.zip"
            Write-Host "Downloading CMake $cmakeVersion..."
            Invoke-WebRequest -Uri $cmakeUrl -OutFile $zipFile -UseBasicParsing
            Write-Host "Extracting..."
            Expand-Archive -Path $zipFile -DestinationPath "C:\" -Force
            Remove-Item $zipFile
            Rename-Item -Path "C:\cmake-$cmakeVersion-windows-x86_64" -NewName "cmake-$cmakeVersion" -Force
          }
          "$cmakeDir\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          $env:Path = "$cmakeDir\bin;$env:Path"
          & cmake --version

      - name: Replace SDK CMake with 3.28 (AGP uses SDK cmake, not system)
        shell: powershell
        run: |
          $sdkCmakeDir = "$env:ANDROID_HOME\cmake\3.22.1\bin"
          $newCmakeDir = "C:\cmake-3.28.3\bin"
          Write-Host "Replacing SDK cmake ($sdkCmakeDir) with CMake 3.28..."
          Copy-Item "$newCmakeDir\cmake.exe" "$sdkCmakeDir\cmake.exe" -Force
          Copy-Item "$newCmakeDir\ctest.exe" "$sdkCmakeDir\ctest.exe" -Force
          Copy-Item "$newCmakeDir\cpack.exe" "$sdkCmakeDir\cpack.exe" -Force
          Copy-Item "$newCmakeDir\ninja.exe" "$sdkCmakeDir\ninja.exe" -Force
          Write-Host "SDK cmake version now: $(& "$sdkCmakeDir\cmake.exe" --version)"
          Write-Host "SDK ninja version now: $(& "$sdkCmakeDir\ninja.exe" --version)"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-home-cache-cleanup: true

      - name: Ensure temp dir
        shell: powershell
        run: New-Item -ItemType Directory -Force -Path C:\tmp | Out-Null

      - name: Ensure Android SDK licenses
        shell: powershell
        run: |
          $licensesDir = Join-Path $env:ANDROID_HOME "licenses"
          New-Item -ItemType Directory -Force -Path $licensesDir | Out-Null
          Set-Content -Path (Join-Path $licensesDir "android-sdk-license") -Value "8933bad161af4178b1185d1a37fbf41ea5269c55`n"
          Set-Content -Path (Join-Path $licensesDir "android-sdk-preview-license") -Value "84831b9409646a918e30573bab4c9c91346d8abd`n"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Generate keystore for signing
        shell: powershell
        run: |
          Remove-Item -Force remotehive-keystore.jks -ErrorAction SilentlyContinue
          keytool -genkey -v -keystore remotehive-keystore.jks -alias remotehive -keyalg RSA -keysize 2048 -validity 10000 -storepass remotehive123 -keypass remotehive123 -dname "CN=RemoteHive, OU=Engineering, O=RemoteHive, L=San Francisco, ST=CA, C=US"

      - name: Prebuild native Android project
        run: npx expo prebuild --platform android

      - name: Clean CMake cache (fix Windows ninja dirty-manifest)
        shell: powershell
        run: |
          Remove-Item -Recurse -Force node_modules/expo-modules-core/android/.cxx -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force node_modules/react-native-reanimated/android/.cxx -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force node_modules/react-native-worklets/android/.cxx -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force android/app/.cxx -ErrorAction SilentlyContinue
          Remove-Item -Recurse -Force android/.cxx -ErrorAction SilentlyContinue

      - name: Create local.properties for CMake suppress regeneration
        shell: powershell
        run: |
          $libs = @(
            "node_modules/react-native-reanimated/android",
            "node_modules/expo-modules-core/android"
          )
          foreach ($lib in $libs) {
            $propsFile = Join-Path $lib "local.properties"
            if (Test-Path $propsFile) {
              $content = Get-Content $propsFile -Raw
              if ($content -notmatch 'CMAKE_SUPPRESS_REGENERATION') {
                Add-Content -Path $propsFile -Value "`ncmake.args=-DCMAKE_SUPPRESS_REGENERATION=ON"
                Write-Host "Updated $propsFile"
              }
            } else {
              Set-Content -Path $propsFile -Value "cmake.args=-DCMAKE_SUPPRESS_REGENERATION=ON" -Encoding ASCII
              Write-Host "Created $propsFile"
            }
          }

      - name: Inject signing config into build.gradle
        shell: powershell
        run: |
          Copy-Item remotehive-keystore.jks android/app/
          node scripts/inject-signing.js android/app/build.gradle

      - name: Build release APK
        shell: powershell
        run: |
          Set-Location android
          cmd /c "gradlew.bat assembleRelease --max-workers 1"
          if ($LASTEXITCODE -ne 0) { throw "Gradle build failed" }

      - name: Rename and prepare APK
        shell: powershell
        run: Copy-Item android/app/build/outputs/apk/release/app-release.apk RemoteHive.apk

      - name: Upload APK to Cloudflare R2
        shell: powershell
        run: |
          curl.exe -s -X PUT "$env:R2_ENDPOINT/$env:R2_BUCKET/RemoteHive.apk" --aws-sigv4 "aws:amz:auto:s3" --user "$env:R2_ACCESS_KEY:$env:R2_SECRET_KEY" -T RemoteHive.apk -w "APK upload: HTTP %{http_code}`n"

      - name: Update version.json in R2
        shell: powershell
        run: |
          $VERSION_CODE = $($env:GITHUB_RUN_NUMBER + 1)
          $VERSION_JSON = '{"version":"1.0.${{ github.run_number }}","versionCode":' + $VERSION_CODE + ',"apkUrl":"https://dl.remotehive.in/latest.apk","releaseNotes":"Build #${{ github.run_number }}"}'
          Set-Content -Path $env:TMP\version.json -Value $VERSION_JSON -Encoding ASCII
          curl.exe -s -X PUT "$env:R2_ENDPOINT/$env:R2_BUCKET/version.json" --aws-sigv4 "aws:amz:auto:s3" --user "$env:R2_ACCESS_KEY:$env:R2_SECRET_KEY" -T $env:TMP\version.json -w "version.json upload: HTTP %{http_code}`n"

      - name: Deploy web app to Cloudflare Workers
        shell: powershell
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npx expo export -p web
          npx wrangler deploy --config wrangler.jsonc

      - name: Regenerate QR code and update README
        shell: powershell
        run: |
          curl.exe -s "https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=https://dl.remotehive.in/latest.apk" -o assets/qr-download.png
          (Get-Content README.md) -replace 'build #[0-9]*', "build #${{ github.run_number }}" | Set-Content README.md
          git config user.name "remotehive-bot"
          git config user.email "bot@remotehive.in"
          git add assets/qr-download.png README.md
          git diff --cached --quiet
          if ($LASTEXITCODE -ne 0) {
            git commit -m "chore: update QR + build number [skip ci]"
            git push
          }
